- content_for :title, 'Home Page'

.row
  .col-sm-1.hidden-xs
  .col-sm-10.col-xs-12
    %h2
      = @user.display_avatar(100,100)
      Hi, #{ @user.firstname }

.row
  .col-sm-1
  .col-sm-10
    %h3 Your Posts
    = will_paginate @posts
    = render @posts
