<%= simple_form_for :post, remote: true do |f| %>
  <%= f.input :title %>
  <%= f.input :description,as: :text, :input_html => { class: 'test', :rows => 10 } %>
  <%= f.button :submit, class: 'button btn btn-primary', "data-dismiss" => "modal", type: "button"%>
<% end %>

