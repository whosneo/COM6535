%br
Sort by
&nbsp;
- if @sort == 'Time⬆️'
  = link_to 'Time ⬆️', request.params.merge(sort: 'Time', order: '⬇️'), class: 'btn btn-default'
- else
  = link_to 'Time ⬇️', request.params.merge(sort: 'Time', order: '⬆️'), class: 'btn btn-default'
- unless defined?(for_replies) && for_replies
  &nbsp;
  - if @sort == 'Comments⬆️'
    = link_to session[:forum_type] == 'App' ? 'Reviews ⬆️️' : 'Comments ⬆️️', request.params.merge(sort: 'Comments', order: '⬇️'), class: 'btn btn-default'
  - else
    = link_to session[:forum_type] == 'App' ? 'Reviews ⬇️' : 'Comments ⬇️', request.params.merge(sort: 'Comments', order: '⬆️'), class: 'btn btn-default'
  - if session[:forum_type] == 'App'
    &nbsp;
    - if @sort == 'Ratings⬆️'
      = link_to 'Ratings ⬆️️', request.params.merge(sort: 'Ratings', order: '⬇️'), class: 'btn btn-default'
    - else
      = link_to 'Ratings ⬇️', request.params.merge(sort: 'Ratings', order: '⬆️'), class: 'btn btn-default'
  - else
    &nbsp;
    - if @sort == 'Likes⬆️'
      = link_to 'Likes ⬆️️', request.params.merge(sort: 'Likes', order: '⬇️'), class: 'btn btn-default'
    - else
      = link_to 'Likes ⬇️', request.params.merge(sort: 'Likes', order: '⬆️'), class: 'btn btn-default'
%br